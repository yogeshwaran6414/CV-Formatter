version: '3.9'
services:
  backend:
    build: ./backend
    ports: ['4000:4000']
    environment:
      MONGO_URI: ${MONGO_URI}
      OPENAI_API_KEY: ${OPENAI_API_KEY}

  frontend:
    build: ./frontend
    ports: ['3000:3000']
    environment:
      NEXT_PUBLIC_BACKEND_URL: http://localhost:4000
